# PYTHON基础
```py
# -*- coding: utf-8 -*-
import datetime
import time
print ("**********************************************************************************************************")
print("当前时间是: ", time.asctime(time.localtime(time.time())))
print ("**********************************************************************************************************")
# 获取当前日期, 注意datetime转string的语法
today = datetime.date.today()
print("今天是: " + today.strftime('%Y-%m-%d'))
print("今天是: " , today.strftime('%Y-%m-%d'), ";" , today.year ,"年",today.month,"月;",today.day,"日;")
# %s %d %f (其中%s是通用的,它将所有格式都转为字符串) 
print("今天是: %s; %s年%s月%s日;" %(today,today.year,today.month,today.day))
print ("只是想告诉你, 在python, 字符串的拼接有三种, 加号, 逗号, 以及 % 替换, 推荐使用后两者")
print ("**********************************************************************************************************")
print ("hello python")
print ("不一定需要用VS Code来写, 也可以用 Git Bush here, 然后键入 python hellopython.py")
print ("Python2文件不支持中文, 中文输入需要utf-8,看前面 # -*- coding: utf-8 -*-")
print ("但python3支持中文")
print ("**********************************************************************************************************")
#在变量类型上, python与JS一样是动态类型的, 不需要声明变量类型
# python的字符串与JS相似, 而转义字符则与C相似
# python也支持像JS一样给函数取别名
a = 3.0
print ("a的值为: ",a)
print ("**********************************************************************************************************")
# 一个打印的技巧, 让语句更简单
y = "今天是: %s; %s年%s月%s日;"
x = y %(today,today.year,today.month,today.day)
print (x)
print ("**********************************************************************************************************")
# 数组
arr = ['A', 'B', 'C', 'D', 'E']
# 注意下面的写法,用%s占位符表示list的某一位时,需要写为 [arr[-1]] 或 (arr[-1],)
print ("数组长度: %d;\n数组的最后一位: %s;\n数组的倒数第二位: %s"
        %(len(arr), [arr[-1]], [arr[-2]]) )
# python的list是动态的,可在末尾插入
arr.append("F")
# list的插入(插入到索引为1的位置)
arr.insert(1, 'insert')
# list删除末尾元素
arr.pop()
# list删除指定位置的元素
arr.pop(1)
# list里面的元素的数据类型也可以不同
arr2 = ['Apple', 123, True]
# list元素也可以是另一个list
arr3 = ['python', 'java', ['asp', 'php'], 'scheme']
# list排序
arr.sort()
# list替换(但并不改变原值,仅仅创建了一个新的对象list,故需要用一个(新/原)变量名来指向他)
str1 = 'abc'
str2 = str1.replace('a','A')
print ("**********************************************************************************************************")
# 元组tuple(tuple的元素是不可变的)
# 创建tuple
tup1 = ('a','b','c')
# 创建只有一个元素的tuple(必须加一个逗号,否则报错)
tup2 = (1,)
print ('[tup1[-1]]会让输出结果也带"[]": ', [tup1[-1]]) 
print (tup2[-1])
print ("**********************************************************************************************************")
# 接收输入 input(), input()括号中的内容表示提示信息
# inputString = input("What is your age? ")
# print (inputString)
# input()返回的是str类型,用int()函数强制转成int类型
print ("**********************************************************************************************************")
# 条件判断if (注意冒号、缩进)
num1 = 25
if num1 >= 50 :
  print ('num1 > 50')
elif num1 >= 25 :
  print ('num1 >= 25')
else :
  print ('num1 <= 50')
print ("**********************************************************************************************************")
# for...in...循环，与while...循环 (注意: 冒号、缩进)
# python也支持break与continue
# range(101)表示从0到100; list()表示将其转换为一个list
sum = 0
sum2 = 0
n = 101
for x in list(range(101)):
  sum = sum + x
while n < 1000 :
  sum2 += n
  n += 1
print('从1到100的和: ',sum,'\n从101到999的和: ',sum2)
print ("**********************************************************************************************************")
# 字典(dict, 即java里的map)
d = {
  'a': 1,
  'b':2
}
print ('字典d["b"]的值为: ',d['b'])
# 向字典d插入新的key-value
d['c'] = 3
print ('字典d["c"]的值为: ',d['c'])
# 判断key是否在字典中的两种方法
print ('判断key“z”是否在字典d中: ','z' in d)
print ('若key"z"字典d中,则返回-100(返回值是自己设定的): ', d.get('z',-100))
# 删除字典中的key
d.pop ('a')
print ('d的内容为: ',d)
print ("**********************************************************************************************************")
# 集合set (只存储key, 不存储value, key是无序且自动被过滤掉重复的)
list1 = [1,2,3,3,3,4]
s = set(list1)
# 添加key
s.add (5)
# 删除key
s.remove (1) 
print ('set()将list1转换为set(将过滤掉重复的): ', s)
s2 = set([5,6])
print ('set相当于数学上的集合,可以做交并集运算: %s (交集); %s (并集);' %(s&s2, s|s2))
print ("**********************************************************************************************************")
# python方法格式(pass表示暂时没想好方法体,使用pass来表示占位)
def myMethod (x):
  pass
def myMethod2 (x):
  if x > 0:
    return '正数'
  elif x == 0:  # 注意是'=='而不是'='
    return '0'
  else:
    return '负数'
print (myMethod2 (-1))
# python方法支持返回多个值, 其实就是返回一个tuple,在接收返回值时时多个变量来接收这个tuple
# python默认参数 (如下,使用power(5)或power(5,3)都是正确的,即使power()是要求两个参数的,但此时你传入一个也不报错,因为默认参数)
# 默认参数必须放在参数列表的最后面
def power(x, n=2):
  s = 1
  while n > 0:
    n = n - 1
    s = s * x
  return s
# python里Null是None
# python可变参数(表示python的参数列表不限个数) def myMethod (*x):
# python命名关键字参数
def myMethod3 (x,y,**kw):
  if 'a' in kw:
    print('a的值为: ',kw) # kw表示输出所有的关键字参数
myMethod3 (1,2,name='xiaoming',age=30,a=4) # 关键字参数的调用必须为这种格式,key=value
print ("**********************************************************************************************************")


# 切片 (原理同JS的slice)
list1 = ['a','b','c','d']
print ('切片为: ',list1[1:3])
# 切片还可以应用于tuple与字符串 ([:4:2]表示前4个数,取第二个; [::2]表示所有数每两个取1个)
print ('tuple切片: ',(1,2,3,4)[:3],'\n字符串切片为: ','abcdef'[:4:2])
print ('-------------------------------------------------------------------------------------')
# 迭代(除了list,tuple,也支持dict,字符串等可迭代对象)
d = {
  'a':1,
  'b':2,
  'c':3,
  'd':4
}
for key in d:
  print ('key: ',key)
for value in d.values():
  print ('value: ',value)
for k,v in d.items():
  print('(%s,%s)' %(k,v))
print ('-------------------------------------------------------------------------------------')
# 列表生成器
l1 = [x * x for x in range(1, 11)]
# 生成结果 [1, 4, 9, 16, 25, 36, 49, 64, 81, 100]
l2 = [x * x for x in range(1, 11) if x % 2 == 0]
# 生成结果 [4, 16, 36, 64, 100]
l3 = [m + n for m in 'ABC' for n in 'XYZ']
# 生成结果 ['AX', 'AY', 'AZ', 'BX', 'BY', 'BZ', 'CX', 'CY', 'CZ']
print(l1,'\n',l2,'\n',l3)
print ('-------------------------------------------------------------------------------------')
# 生成器
```